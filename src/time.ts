/**
 * Copyright (c) 2026 Nicholas Waltz
 *
 * time.ts
 */

export const secToMs = (sec: number) => sec * 1000;
export const msToSec = (ms: number) => ms / 1000;

export const secToUs = (sec: number) => sec * 1_000_000;
export const usToSec = (us: number) => us / 1_000_000;
